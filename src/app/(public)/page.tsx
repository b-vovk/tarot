'use client';

import TarotSpread from '@/components/TarotSpread';

export default function HomePage() {
  return (
    <main className="flex items-center justify-center min-h-screen bg-gradient-to-b from-purple-100 to-white p-4">
      <TarotSpread />
    </main>
  );
}